from setuptools import setup, find_packages

setup(
    name="llm-brochure-maker",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add your requirements here
    ],
)